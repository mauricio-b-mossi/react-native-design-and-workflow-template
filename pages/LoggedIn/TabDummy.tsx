import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Tab = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text style={{fontWeight: '400'}}>Tab</Text>
        </View>
    )
}

export default Tab

const styles = StyleSheet.create({})
